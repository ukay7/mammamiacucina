<div class="mmc-cart-layout"><div>@forelse($cart['items'] as $item)@php($p=$item['product'])<article class="mmc-cart-row">@if($image=$p->media->first())<img src="{{ route('catalogue.media',[$p,$image]) }}" alt="{{ $p->premium_marketing_name }}">@else<span class="mmc-cart-placeholder">Image coming soon</span>@endif<div><a href="{{ route('catalogue.product',$p->slug) }}"><h3>{{ $p->premium_marketing_name }}</h3></a><p>${{ number_format($item['unit_cents']/100,2) }} each · ${{ number_format($item['line_cents']/100,2) }}</p></div>@include('partials.cart-item-controls')</article>@empty<p>Your cart is empty.</p>@endforelse<a class="mmc-text-link" href="{{ route('theme.product-grid') }}">← Continue Shopping</a></div><aside class="mmc-panel"><h2>Order Summary</h2><div class="mmc-summary-line"><span>Subtotal</span><strong>${{ number_format($cart['total']/100,2) }}</strong></div><p class="mmc-note">Delivery charges and tax will be confirmed separately. Payment is cash on delivery.</p>@if($cart['count'])<a class="mmc-button" href="{{ route('theme.checkout') }}">Proceed to Checkout</a>@endif</aside></div>