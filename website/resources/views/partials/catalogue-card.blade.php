<article class="mmc-product-card ps-product"><div class="mmc-product-visual"><a href="{{ route('catalogue.product',$product->slug) }}"><div class="mmc-product-image">@if($image=$product->media->first())<img src="{{ route('catalogue.media',[$product,$image]) }}" alt="{{ $product->premium_marketing_name }}" loading="lazy" decoding="async" width="400" height="320">@else<div class="mmc-shop-no-image">Image coming soon</div>@endif</div></a>@if($product->total_selling_price_cad!==null)<form data-live-cart-form class="mmc-hover-cart" method="post" action="{{ route('cart.add',$product) }}">@csrf<input type="hidden" name="quantity" value="1"><button type="submit" aria-label="Add {{ $product->premium_marketing_name }} to cart" title="Add to Cart">@include('partials.cart-icon')</button></form>@endif</div><a href="{{ route('catalogue.product',$product->slug) }}"><h3>{{ $product->premium_marketing_name }}</h3></a><p class="mmc-price">{{ $product->total_selling_price_cad===null?'PRICE ON REQUEST':'$'.number_format($product->total_selling_price_cad,2) }}</p><a class="mmc-text-link" href="{{ route('catalogue.product',$product->slug) }}">View Product →</a></article>