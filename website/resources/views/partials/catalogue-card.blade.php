<article class="mmc-product-card ps-product"><a href="{{ route('catalogue.product',$product->slug) }}"><div class="mmc-product-image">@if($image=$product->media->first())<img src="{{ route('catalogue.media',[$product,$image]) }}" alt="{{ $product->premium_marketing_name }}" loading="lazy" decoding="async" width="400" height="320">@else<div class="mmc-shop-no-image">Image coming soon</div>@endif</div><h3>{{ $product->premium_marketing_name }}</h3></a><p class="mmc-price">{{ $product->total_selling_price_cad===null?'PRICE ON REQUEST':'$'.number_format($product->total_selling_price_cad,2) }}</p><a class="mmc-text-link" href="{{ route('catalogue.product',$product->slug) }}">View Product →</a></article>
