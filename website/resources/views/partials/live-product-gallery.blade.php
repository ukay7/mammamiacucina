<div class="mmc-live-gallery" data-live-gallery><div class="mmc-live-stage">@forelse($product->media as $media)<div data-live-slide @if(!$loop->first) hidden @endif>@if($media->kind==='image')<img src="{{ route('catalogue.media',[$product,$media]) }}" alt="{{ $product->premium_marketing_name }} — photo {{ $loop->iteration }}" @if(!$loop->first) loading="lazy" @endif>@else<video controls preload="none" src="{{ route('catalogue.media',[$product,$media]) }}"></video>@endif</div>@empty<div class="mmc-shop-no-image">Image coming soon</div>@endforelse</div>
@if($product->media->count()>1)<div class="mmc-live-thumbs" role="group" aria-label="Product images and videos">@foreach($product->media as $media)<button type="button" data-live-thumb="{{ $loop->index }}" aria-label="Show {{ $media->kind }} {{ $loop->iteration }}" aria-pressed="{{ $loop->first?'true':'false' }}">@if($media->kind==='image')<img src="{{ route('catalogue.media',[$product,$media]) }}" alt="" loading="lazy">@else<span aria-hidden="true">▶</span><small>Video</small>@endif</button>@endforeach</div>@endif<p class="mmc-note" data-gallery-caption aria-live="polite">{{ $product->media->count() ? '1 / '.$product->media->count() : '' }}</p></div>
