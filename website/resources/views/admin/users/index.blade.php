@extends('admin.layout')
@section('title','User Management')
@section('content')
<div class="panel"><div class="panel-hdr"><h2>Team <span>Accounts</span></h2>@if(auth()->user()->hasAdminPermission('users.manage'))<a href="{{ route('admin.users.create') }}" class="btn btn-primary ml-auto mr-3">Add User</a>@endif</div><div class="panel-container show"><div class="panel-content"><form method="get" class="d-flex mb-4"><label class="sr-only" for="search">Search users</label><input id="search" name="search" class="form-control mr-2" value="{{ $search }}" placeholder="Search by name or email"><button class="btn btn-outline-primary">Search</button></form><div class="table-responsive"><table class="table table-hover"><thead><tr><th>Name</th><th>Email</th><th>User Type</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($users as $user)<tr><td>{{ $user->name }}</td><td>{{ $user->email }}</td><td>{{ $user->role?->name ?? 'Unassigned' }}</td><td><span class="badge {{ $user->is_active?'badge-success':'badge-secondary' }}">{{ $user->is_active?'Active':'Inactive' }}</span></td><td>@if(auth()->user()->hasAdminPermission('users.manage') && !$user->role?->is_super && !array_diff($user->permissions(),auth()->user()->permissions()))<a class="btn btn-sm btn-outline-primary" href="{{ route('admin.users.edit',$user) }}">Edit</a>@else<span class="text-muted">{{ $user->role?->is_super ? 'Protected' : 'View only' }}</span>@endif</td></tr>@empty<tr><td colspan="5" class="text-center py-5">No users found.</td></tr>@endforelse</tbody></table></div>{{ $users->links('pagination::bootstrap-4') }}</div></div></div>
@endsection