@extends('layouts.mmc-page',['pageTitle'=>'Product Details'])
@section('page-content')
<a class="mmc-text-link" href="{{ route('theme.product-grid') }}">← Back to Products</a>
<section class="mmc-two-col ps-product--detail"><div class="mmc-live-media">@forelse($product->media as $media)@if($media->kind==='image')<img src="{{ route('catalogue.media',[$product,$media]) }}" alt="{{ $product->premium_marketing_name }}" loading="lazy" style="width:100%;height:auto">@else<video src="{{ route('catalogue.media',[$product,$media]) }}" controls preload="metadata" style="width:100%"></video>@endif @empty<div class="mmc-shop-no-image">Image coming soon</div>@endforelse</div><div><p class="mmc-eyebrow">MAMMA MIA CUCINA</p><h2>{{ $product->premium_marketing_name }}</h2>@if($product->italian_subtitle)<p>{{ $product->italian_subtitle }}</p>@endif<p class="mmc-price">{{ $product->total_selling_price_cad===null?'PRICE ON REQUEST':'$'.number_format($product->total_selling_price_cad,2) }}</p><p>Contact us for serving sizes, ingredients, allergens and availability.</p><a class="mmc-button" href="{{ route('theme.contact') }}">Enquire About This Product</a><div class="mmc-detail-meta">Categories: @foreach($product->categories as $category)<a href="{{ route('theme.product-grid',['category'=>$category->slug]) }}">{{ $category->name }}</a>{{ $loop->last?'':', ' }}@endforeach</div></div></section>
@endsection
