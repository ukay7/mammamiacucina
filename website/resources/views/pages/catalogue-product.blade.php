@extends('layouts.mmc-page',['pageTitle'=>'Product Details'])
@section('page-content')
<a class="mmc-text-link" href="{{ route('theme.product-grid') }}">← Back to Products</a>
<section class="mmc-two-col mmc-live-detail ps-product--detail">@include('partials.live-product-gallery')<div><p class="mmc-eyebrow">MAMMA MIA CUCINA</p><h2>{{ $product->premium_marketing_name }}</h2>@if($product->italian_subtitle)<p>{{ $product->italian_subtitle }}</p>@endif<p class="mmc-price">{{ $product->total_selling_price_cad===null?'PRICE ON REQUEST':'$'.number_format($product->total_selling_price_cad,2) }}</p><p>Contact us for serving sizes, ingredients, allergens and availability.</p>@if($product->total_selling_price_cad!==null)<form data-live-cart-form class="mmc-purchase" method="post" action="{{ route('cart.add',$product) }}">@csrf<label for="quantity">Quantity</label><input id="quantity" name="quantity" type="number" min="1" max="99" value="1" required><button class="mmc-button" type="submit">Add to Cart</button></form><p data-cart-feedback role="status"></p>@else<a class="mmc-button" href="{{ route('theme.contact') }}">Enquire About This Product</a>@endif<div class="mmc-detail-meta">Categories: @foreach($product->categories as $category)<a href="{{ route('theme.product-grid',['category'=>$category->slug]) }}">{{ $category->name }}</a>{{ $loop->last?'':', ' }}@endforeach</div></div></section>
@endsection
